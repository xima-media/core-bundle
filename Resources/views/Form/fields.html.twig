{#

This file is part of the Sonata package.

(c) Thomas Rabaix <thomas.rabaix@sonata-project.org>

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.

#}

{% block xima_type_time_picker_widget %}
    {% spaceless %}
        <div class="form-group">
            <div class='input-group date' id='{{ id }}' data-date-format="{{ moment_format }}">
                {{ block('form_widget_simple') }}
                <span class="input-group-addon"><span class="glyphicon glyphicon-time"></span>
                </span>
            </div>
        </div>
        <script type="text/javascript">
            moment.locale('en', {
                week: { dow: 1 } // Monday is the first day of the week
            });

            jQuery(function ($) {
                $('#{{ id }}').datetimepicker({{ dp_options|json_encode|raw }});
            });
        </script>
    {% endspaceless %}
{% endblock xima_type_time_picker_widget %}

{% block xima_type_date_picker_widget %}
    {% spaceless %}
        <div class="form-group">
            <div class='input-group date' id='{{ id }}' data-date-format="{{ moment_format }}">
                {{ block('form_widget_simple') }}
                <span class="input-group-addon"><span class="glyphicon glyphicon-time"></span>
                </span>
            </div>
        </div>
        <script type="text/javascript">
            jQuery(function ($) {
                $('#{{ id }}').datetimepicker({{ dp_options|json_encode|raw }});
            });
        </script>
    {% endspaceless %}
{% endblock xima_type_date_picker_widget %}
